# v0.4.1 - 外部ログメッセージ登録機能の追加

## 概要

ATACを使用するプロジェクトが独自のログメッセージキーを登録できるよう、`registerLogMessages()` 関数を公開した。

## 変更内容

### 追加
- `src/messages/logger.ts` - `registerLogMessages(messages: Record<string, string>)` 関数
- `src/index.ts` - `registerLogMessages` のエクスポート

### 変更なし（破壊的変更なし）
